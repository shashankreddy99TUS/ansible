- name: install and run nginx
 hosts: web
 become: yes # -b
 tasks:
 - name: install nginx
 ansible.builtin.package:
   state: nginx 
 - name: start the nginx
   ansible.bultin.service:
   name: nginx
   state: started  