- name: install and run nginx
 hosts: web
 become: yes # -b
 tasks:
 - name: install nginx
 ansible.builtin.package:
   state: present
 - name: start the nginx
   ansible.bultin.service:
   name: nginx
   state: started  